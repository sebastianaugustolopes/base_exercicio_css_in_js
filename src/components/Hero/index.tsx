import { StyledForm, FormContainer, HeroTitle } from './Hero.styles';

const Hero = () => (
  <StyledForm>
    <FormContainer className="container">
      <HeroTitle>
        As melhores vagas para tecnologia, design e artes visuais.
      </HeroTitle>
    </FormContainer>
  </StyledForm>
);

export default Hero;
